---
import Layout from '../../layouts/Layout.astro'
import { WebComponentsFrame } from '../../component/WebComponentsFrame'

const { id } = Astro.params
---

<Layout>
  {id && <WebComponentsFrame client:load id={id} />}
</Layout>
